# Production Frontend Environment Configuration (for Vercel / Cloudflare Pages)
# Replace with your deployed backend URL (Render, Railway, or Fly)
#
# NOTE: the currently-live Render backend host is `tradetron-8jkz.onrender.com`
# (the legacy `tradetron-backend.onrender.com` host is dead — returns 404).

VITE_API_URL=https://tradetron-8jkz.onrender.com
VITE_WS_URL=wss://tradetron-8jkz.onrender.com
